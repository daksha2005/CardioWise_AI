import React from 'react';
import ReactDOM from 'react-dom/client';
import './CardioWise.css';
import CardioWiseApp from './CardioWiseApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CardioWiseApp />
  </React.StrictMode>
);
